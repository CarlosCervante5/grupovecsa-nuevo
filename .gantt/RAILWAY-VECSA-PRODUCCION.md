# VECSA nuevo — producción Railway (2026-06-02)

Despliegue funcional verificado:

- Backend: `vecsa-backend-production.up.railway.app` (migrate, módulo Legales, API `/api/legal/*` y `/api/admin/legal/*`)
- Frontend: `vecsa-frontend-production.up.railway.app` (build `npm run prod`, `serve.json`, admin Legales)

Repos de código desplegados: `Strega-ABVecsa-Developments/vecsa-backend` y `vecsa-frontend` (ramas `grupovecsa*Railway`).

Tarea Gantt: **#4** — Pasar a producción VECSA nuevo.
